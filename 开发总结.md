# 电赛开发记录：优化 / 纠错 / 策略选择
                                                                                                                        f 按时间顺序整理，标注每条的类别：
  - 🔧 纠错（bug 修复）
  - ⚡ 优化（性能/稳定性提升）
  - 🎯 策略选择（方案决策）

  ---

  ## 一、24 年 H 题循迹小车阶段

  ### 1. 从单文件到模块化 🎯
  **问题**：所有代码堆在一个 main.c，改动一个参数要在几千行里找。
  **决策**：拆成 sensor.c / motor.c / pid.c / state_machine.c / gyro.c / main.c。
  **价值**：改动范围可控，代码成为可复用的资产。

  ### 2. 增量式 PID →位置式 PID 🔧
  **问题**：增量式 PID 的 I 项写成了位置式累计积分，P/D 又是增量式差分，混用导致输出锁死；D 项第一帧爆炸（误差从 0
  突变为 pos，d_out = pos/dt 巨大）。
  **决策**：改用位置式 PID，返回值直接是绝对值，不再 `pid_out += inc` 累加。
  **价值**：第一帧不再炸，输出不再锁死，调参直观。

  ### 3. d_filt 静态变量串扰 🔧
  **问题**：`static float d_filt` 写在 pid_compute 函数里，两个 PID 实例（循迹 + 角度）共用同一个变量，D
  项互相污染，入弯方向随机。
  **决策**：把 d_filt 移到 PID_t 结构体里，每个实例独立。
  **价值**：首次入弯行为稳定一致，不再"有时候左转有时候右转"。

  ### 4. 二值传感器 + PID →三区开关控制 🎯
  **问题**：二值灰度传感器位置只有约 15 个离散值（步长 7），喂给 PID 导致 P/D 项在离散跳变上疯狂振荡，"直-顿-直-顿"。
  **决策**：放弃 PID，改用三区开关——左区右转、中区直走、右区左转，极端区不触发（让弧线自身曲率回弹）。
  **价值**：二值传感器终于能稳定循迹，调参从 4 个参数简化到 1 个差速量。

  ### 5. 换 ADC 灰度传感器 →回到 PD + EMA 🎯
  **问题**：二值传感器做不到连续平滑的弧线跟踪。
  **决策**：换成 8 路 12 位 ADC 灰度传感器，用加权重心法算连续位置，加 EMA 低通滤波（0.6/0.4），用纯 PD（无积分）循迹。
  **价值**：位置从 7 步跳变变成 0.05 步渐变，PD 不再炸，弧线跟踪平滑。

  ### 6. g_sensor_buf 数据过期 🔧
  **问题**：传感器缓存只在 line_follow() 里刷新，盲走模式下 200ms 才更新一次，导致入弯时读的是旧数据，车冲过黑线。
  **决策**：主循环每帧强制 `sensor_read_all()` 刷新缓存。
  **价值**：消除 200ms 数据过期，入弯响应及时。

  ### 7. entered/exited 中间态盲区 🔧
  **问题**：line_now 判据是 ≥2路黑，fully_off 判据是 ==0 路黑，"1 路黑"是盲区，出弯检测永远凑不齐条件。
  **决策**：用 was_on_line 标记（见过线就记住，只在真正出弯处理完才清），且把 line_now 阈值改为 ≥1路。
  **价值**：出弯停车 + 调整逻辑终于能触发。

  ### 8. 盲走目标角每帧重设 →只设一次 🔧
  **问题**：blind_drive 每帧 `gyro_set_target_yaw`，目标反复重置导致方向修正不稳定。
  **决策**：目标角在段切换时设一次，盲走函数只读误差做 P 控制。
  **价值**：盲走方向稳定。

  ### 9. 步进电机/舵机不适用，D36A 开环步进 + 原地转角 🎯
  **问题**：出弯靠陀螺仪闭环转角不可靠。
  **决策**：改成原地开环差速转角（固定帧数），转完锁当前朝向盲走。陀螺仪不负责转角度，只负责保直线。
  **价值**：出弯方向可控，不再依赖陀螺仪闭环追角度。

  ---

  ## 二、通信 / 传感器硬件阶段

  ### 10. 蓝牙波特率不匹配 🔧
  **问题**：软件模拟 UART 发 115200，蓝牙模块默认 9600，上位机收不到。
  **决策**：改硬件 UART，波特率对齐 9600。
  **价值**：蓝牙调试数据能稳定上传。

  ### 11. 软件模拟 UART →硬件 UART 🎯
  **问题**：软件 bit-bang 关全局中断，影响控制周期。
  **决策**：改用硬件 UART，不占 CPU、波特率准。
  **价值**：通信不影响 PID 实时性。

  ### 12. 视觉 UART 中断名错误 🔧
  **问题**：写了 `UART1_IRQHandler`，但 MSPM0 SDK 的命名是 `UART_1_INST_IRQHandler`，链接器把函数当普通函数，中断走
  Default_Handler，坐标永远收不到。
  **决策**：改成正确的 `UART_1_INST_IRQHandler`。
  **价值**：视觉坐标终于能收到。

  ### 13. BMI160 软件 I2C 上拉被覆盖 🔧
  **问题**：`DL_GPIO_initDigitalOutput` 会覆盖 IOMUX 寄存器全部字段，把之前配的 SDA 上拉清掉，输入模式时 SDA 浮空，I2C
  100% 失败。
  **决策**：SDA 改开漏输出，不切输入模式，只改方向位。
  **价值**：这是"100% 失败 vs 时序问题"的关键区分——电气层不通。

  ### 14. 换单轴陀螺仪模块 🎯
  **问题**：BMI160 软件 I2C 太脆弱。
  **决策**：换成 UART 单轴陀螺仪模块，ISR 自动更新 yaw，115200 波特率。
  **价值**：陀螺仪数据稳定可靠。

  ---

  ## 三、26 年 H 题车载平衡滚球阶段

  ### 15. 任务3 开环 flick + PID 锁定 🎯
  **问题**：球从中心去 ±5cm需要主动的加减速运动轮廓，纯 PID 追不上惯性。
  **决策**：开环 flick（固定脉冲序列甩球）先把球送到目标附近，再用 PID 精确定位锁定。
  **价值**：球能在 5s 内完成 O→+5→-往返。

  ### 16. 步进电机启动抖动 🔧
  **问题**：电机通电瞬间转子被磁场拉到最近磁极，产生一个"咔"的跳动。
  **决策**：使能后延迟 200ms 等励磁稳定再发脉冲。
  **价值**：消除启动抖动。

  ### 17. 死区归零 →死区自激 🔧
  **问题**：PID 输出死区内归零（step_pos 回到 0），归零动作扰动球 →再次触发 PID →循环自激。
  **决策**：死区内不强制归零，改为球到目标且稳定后才处理。
  **价值**：消除平衡点附近的微振。

  ### 18. 任务6 震荡卡 0（关键 bug）🔧
  **问题**：不管目标设在哪，球震荡过程中总卡在中心 0。
  **根因**：步数限位用了 `t6_err_abs = fabsf(b6_cm)`（绝对位置），而不是误差。球过中心时 b6_cm≈0→限位≈0→
  输出被掐死。任务 5 目标恒为 0，位置和误差碰巧相等，bug 藏了一整天；任务 6 目标非 0 一测就炸。
  **决策**：改成 `fabsf(b6_cm - t6_target)`。
  **价值**：这是整场比赛最值钱的发现——"参数在特定条件下才暴露 bug"。

  ### 19. 车体运动前馈 bias 试了又删 🎯
  **问题**：车加速时球因惯性相对滑动。
  **决策**：试了 bias +7/-7/-4/-5/-3 六档，最终全部归零，靠 PD 的 D 项（速度阻尼）自己抗惯性扰动。
  **价值**：认识到"前馈是给 PID 一个起点，不是精确锁球"；试错本身就是产出。

  ### 20. 渐变偏置逻辑试了又删 🎯
  **问题**：球目标偏离中心越远，反方向限制越紧。
  **决策**：试了 ×20/×10/×5/×2/×0.8/六档，最后决定去掉，统一 |e|×50上限 70。
  **价值**：复杂逻辑不带来收益就砍掉，保持控制简单。

  ### 21. 多任务 PID 实例独立 🎯
  **问题**：任务 3 和任务 4/5/6 的控制目标不同（静止往返 vs 动态平衡）。
  **决策**：任务 3 用独立实例（死区 0.5、I 限幅 200），任务 4/5/6 统一参数（死区 0.1-0.2）。
  **价值**：各任务互不干扰，调参不互相污染。

  ---

  ## 四、核心经验总结

  1. **二值传感器用开关控制，模拟量传感器用 PID。** 硬件决定算法，别硬套。
  2. **位置式 PID 比增量式直观，调参不容易出错。**
  3. **D 项搭在速度上（-Kd×v），不要搭在位置差分上**——速度更平滑，噪声小。
  4. **static 变量在函数里会跨实例串扰**，状态要放进结构体。
  5. **中断名、IOMUX 编号、波特率**这些"配置类"问题，现象是"100% 失败"，不是"间歇性"。
  6. **开环步进有启动失步和换向丢步的物理上限**，闭环步进（带编码器）是质变，不是调参能跨越的。
  7. **位置和误差在目标为 0 时碰巧相等**——这 bug 只在目标非 0 时才暴露，要主动测非零目标。
  8. **试了又删的东西不是白费**，那是排除了错误方向，留下的才是对的。
